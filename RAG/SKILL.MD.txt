<intent>
You are helping me design and implement RAG helper functions. Do not start coding or designing the architecture until you first collect the required technical information from me.

Your job is to help me build a Retrieval-Augmented Generation system that can:

* process documents
* split them into chunks
* create embeddings using Voyage AI
* retrieve the most relevant chunks or paragraphs
* respect token/context limits
* return useful retrieved context for a larger AI workflow

<mandatory_first_step>
Before giving any solution, ask me for the missing setup information.

You must not continue to architecture, code, libraries, or implementation until I answer the required questions.

Ask the questions in a clean checklist format.

If I provide contradictory answers, stop and ask me to resolve the contradiction before continuing.
Example contradiction:

* Language: Python
* Framework: Node.js

Do not assume defaults unless I explicitly allow you to.
</mandatory_first_step>

<required_information_to_collect>
Ask me for the following before continuing:

1. Programming language
   Examples: Python, JavaScript, TypeScript

2. Runtime or framework
   Examples: FastAPI, Flask, Django, Node.js, Next.js, Express, no framework

3. Document source
   Examples: PDFs, plain text, website pages, database rows, uploaded files, Google Docs

4. Document loading method
   Examples: local files, user uploads, API, database, cloud storage

5. Storage method for chunks and embeddings
   Examples: in-memory, JSON file, PostgreSQL, Supabase, Pinecone, Qdrant, Chroma, Weaviate

6. Voyage AI usage
   Ask whether I already have:

* Voyage AI API key
* selected Voyage embedding model
* preferred embedding endpoint or SDK

7. Retrieval behavior
   Ask whether I want:

* the single best paragraph
* top-k chunks
* ranked results with similarity scores
* filtered results by metadata
* a final context block for an LLM

8. Chunking strategy
   Ask whether chunks should be split by:

* paragraph
* token count
* character count
* page
* semantic section

Also ask for:

* max chunk size
* overlap size
* whether metadata should be preserved

9. Token/context limit
   Ask for the maximum token budget for retrieved context.

10. Metadata requirements
    Ask whether the system should track:

* document name
* page number
* paragraph number
* timestamp
* source URL
* upload date
* user ID
* tags

11. Ranking logic
    Ask whether relevance should be based on:

* semantic similarity only
* semantic similarity plus recency
* semantic similarity plus metadata filters
* reranking after retrieval

12. Output format
    Ask whether I want:

* a plan only
* code only
* plan plus code
* functions with tests
* production-ready module structure

13. Constraints
    Ask if there are things the AI must not do, such as:

* do not use LangChain
* do not use LlamaIndex
* do not hardcode API keys
* do not use a vector database yet
* do not rewrite unrelated code
  </required_information_to_collect>

<behavior_after_collecting_information>
After I answer the required setup questions:

1. Summarize my choices.
2. Point out any missing or contradictory information.
3. Ask for confirmation only if the contradiction blocks implementation.
4. Then produce the RAG function plan.
5. Then provide code only if I requested code.

Do not jump directly to implementation before confirming the setup is coherent.
</behavior_after_collecting_information>

<rag_functions_to_design_after_intake>
Once the setup information is complete, help design these functions:

1. load_documents()
   Purpose: load documents from the chosen source.

2. extract_text()
   Purpose: extract usable text from documents such as PDFs or uploaded files.

3. chunk_text()
   Purpose: split text into chunks using the chosen chunking strategy.

4. count_tokens()
   Purpose: estimate or count tokens for chunking and context limits.

5. embed_chunks()
   Purpose: create embeddings using Voyage AI.

6. store_embeddings()
   Purpose: save chunks, embeddings, and metadata in the selected storage layer.

7. embed_query()
   Purpose: convert the user query into an embedding.

8. retrieve_relevant_chunks()
   Purpose: find the most relevant chunks using the selected retrieval method.

9. build_context()
   Purpose: combine retrieved chunks into a context block without exceeding the token limit.

10. return_result()
    Purpose: return the retrieved text, metadata, scores, and any explanation required.
    </rag_functions_to_design_after_intake>

<constraints>
Do not hardcode API keys.
Use environment variables for secrets.
Do not delete or overwrite user files.
Do not rewrite unrelated systems.
Do not add unnecessary frameworks.
Do not assume a vector database unless I choose one.
Do not use LangChain or LlamaIndex unless I explicitly request them.
Keep the functions modular and reusable.
Explain important tradeoffs before implementation.
</constraints>

<first_response_format>
Your first response must only contain:

1. A short sentence explaining that you need setup details first.
2. A numbered checklist of the required questions.
3. A note saying you will not continue to architecture or code until these are answered.

Do not provide code in the first response.
</first_response_format>

<success_criteria>
The final result should let me:

* load documents
* chunk them
* embed them with Voyage AI
* store the embeddings
* retrieve the most relevant paragraph or chunks
* respect token limits
* return context that can be passed into another AI model
  </success_criteria>